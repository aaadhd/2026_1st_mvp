# Netlify 설정 파일
# Vite 프로젝트 배포 설정

[build]
  # Vite 빌드 명령어
  command = "npm run build"
  
  # Vite 빌드 결과물 디렉토리
  publish = "dist"

# 리다이렉트 설정 (SPA를 위한 fallback)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
