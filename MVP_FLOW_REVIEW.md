# MVP 플로우 종합 점검 (UI/UX · 서비스 · 사업성)

**검토일**: 2026-01-27  
**대상**: 신나는 그림약방 1차 MVP  
**기준**: PRD v0.4 검증 가설 및 Success Criteria

---

## 1. 현재 앱 플로우 맵

```
INTRO (인트로)
   ↓ [오늘의 아트 세션 시작]
TUNING (밸런스 게임 7단계) ← 뒤로가기 시 이전 단계 (선택 복원 표시)
   ↓ 7번째 선택 후
LOADING (매칭 중…)
   ↓ 자동 또는 스킵
RESULT (오늘의 아트 메이트)
   ├─ [화가와 아트 세션 시작하기] → 체크박스 ON: CLIP_INTRO → MASTERPIECE → PLAYING
   │                                체크박스 OFF: 바로 PLAYING
   ├─ [선택 다시하기] → TUNING (처음부터)
   └─ [아트 게임 모아보기] → HUB

CLIP_INTRO (명화 클립) → [아트 게임 이어하기] → PLAYING
MASTERPIECE (작품 감상 전체화면) ← CLIP_INTRO에서 이미지 터치 시

PLAYING (게임) → 완료/실패 시
   ↓
GAME_RESULT (게임 종료!)
   ├─ [다시 도전하기] → PLAYING
   ├─ [추천 게임] → HUB
   └─ [정서/인지/사회/감각] 추천 게임 가로 스크롤 → playNext(id) → CLIP_INTRO 경로

HUB (아트 게임 모아보기)
   ├─ [명화 클립 같이 보기] 체크박스 (ON: 감상 후 게임 / OFF: 게임만)
   ├─ 오늘의 추천 아트 게임 (노란 카드) → startLevelIntro()
   ├─ 전체 / 정서 / 인지 / 사회 / 감각 탭
   └─ 게임 카드 클릭 → playNext(id) → startLevelIntro() → CLIP_INTRO or PLAYING
```

---

## 2. UI/UX 관점

### 잘 되어 있는 점
- **스크리닝 → 피드백 → 액티비티** 3단계 퍼널이 코드/화면에 선형으로 구현됨.
- **튜닝 뒤로가기**: 2~7단계에서 이전 단계 복귀 + “내가 고른 것” 표시로 선택 번복이 가능함.
- **명화 클립 옵션**: Hub·Result 진입 경로 모두에서 “명화 클립 같이 보기” 체크로 감상 유무를 선택할 수 있음.
- **결과 화면**: “게임 종료!” + “수식어+화가와/과 함께한 시간” 등 문구가 PRD/이미지 톤과 맞음.
- **프로그레스**: 튜닝 상단에 “N/7”·프로그레스 바로 완료 유도.
- **게임 인트로**: 튜토리얼 모달로 조작법 안내 후 진입.

### 개선이 필요한 점
1. **Result → 세션 시작 시 분기**
   - “명화 클립 같이 보기”는 Hub에서만 저장됨. Result에서 “화가와 아트 세션 시작하기”를 누를 땐 **항상** `showMasterpieceClip` 상태를 사용하는데, 사용자는 Result에서는 이 체크를 못 봄.  
   - **권장**: Result에서도 “이번만 감상 건너뛰기” 같은 한 번만의 옵션을 두거나, Result와 Hub에서 “명화 클립 같이 보기” 의미가 같은지 문구로 맞추기.

2. **GAME_RESULT 이후 경로**
   - “다시 도전하기”는 같은 화가·같은 게임으로 바로 PLAYING.
   - “추천 게임”은 HUB로만 감. 같은 화가의 다른 게임으로 바로 갈 수 있는 숏컷이 없음.  
   - MVP에서는 “추천 게임 가로 스크롤 → 카드 선택”으로 충분할 수 있으나, “한 판 더 vs 다른 게임” 인지가 더 분명하면 좋음.

3. **일관된 용어**
   - “아트 세션”, “아트 게임”, “추천 게임”, “세션”이 혼용됨.  
   - 타깃(55~75세)을 위해 한 화면 안에서는 같은 개념은 같은 단어로 통일하는 편이 이해도에 유리함.

4. **접근성**
   - 버튼·카드 터치 영역, 폰트 크기는 시니어 가이드와 한 번 더 맞춰볼 여지가 있음 (PRD에 “시니어 대상” 명시됨).

---

## 3. 서비스(플로우·로직) 관점

### 잘 되어 있는 점
- **스크리닝 가중치**: 7쌍 선택 → 4개 영역 가중치 → 1순위 영역 내 랜덤 화가. “취향 발견” 프레이밍과 맞음.
- **단계별 선택 저장**: `tuningSelectionByStep`로 단계별 선택 보존, 뒤로가기 시 “이전에 고른 것” 표시 및 가중치 롤백이 됨.
- **로딩 타이머**: 메시지 순환 + 백업 타이머 + 스킵 시 정리까지 되어 있어, LOADING 구간에서 멈추는 위험은 줄어듦.
- **에러·토스트**: 앱 프레임(`#app`) 안에서 노출되도록 되어 있어, 서비스가 “앱 안”에 머무른다는 인상과 맞음.

### 정리할 데드 코드
- **EMOTION_DIARY / DIARY_LIST**
  - `goToDiary`, `goToDiaryList` 제거로 이 화면들로 들어가는 경로가 없음.
  - `script.js` render에는 `EMOTION_DIARY` / `DIARY_LIST` 분기와 `EmotionDiaryScreen` / `DiaryListScreen`이 남아 있음.
  - **권장**: “감정 일기”를 서비스에서 제외했다면, 두 화면 분기 및 관련 import/step를 제거하고, `skipFlow`의 `EMOTION_DIARY` 등도 정리.

### 플로우 꼬임 가능성
- **HUB → 오늘의 추천 카드**
  - `startLevelIntro()` 호출 시 `state.persona`는 “오늘의 아트 메이트”로 이미 정해진 상태.  
  - HUB에서 “다른 세션”을 눌러 들어온 경우, “오늘의 추천”이 “현재 결과 화가의 게임”이므로, “오늘의 추천 아트 게임” 문구와 의미가 맞음. 다만 “다른 게임만 보고 싶은” 사용자는 탭/그리드로 빠르게 갈 수 있게만 해도 될 수준.

---

## 4. 사업성·제품(MVP 가설) 관점

### PRD 검증 가설 대비
| 가설 | 내용 | 현재 구현 상태 |
|------|------|-----------------|
| 가설 1 | 밸런스 게임 형태 스크리닝 → 거부감 없이 다음 단계 진입 | ✅ 7단계 VS 선택, 뒤로가기·선택 표시, 프로그레스 바 |
| 가설 2 | 결과 기반 추천 → 이유 이해 + 액티비티 전환 | ✅ 매칭 이유 문구, “화가와 아트 세션 시작하기” 등 CTA |
| 가설 3 | 디지털 아트 액티비티 → 기본 몰입·성취감·완료 유도 | ✅ 레벨업, 제한시간, 점수, 게임 종료 화면, “다시 도전하기” 등 |

### Success Criteria 측정 가능 여부
- **스크리닝 완료율**  
  - `screening_complete` 등 이미 로그가 있으면, “시작 시점 대비 완료 시점” 비율로 추산 가능.  
  - “완료”를 “7번째 선택 후 LOADING 진입”으로 정의하면 플로우와 일치.
- **스크리닝 → 피드백 전환율**  
  - LOADING → RESULT 진입이 곧 피드백 노출이므로, 위 완료 이후 RESULT view 비율로 측정 가능.
- **피드백 → 활동 진입율**  
  - RESULT에서 “화가와 아트 세션 시작하기” 클릭 또는 `startLevelIntro` 호출 → PLAYING/CLIP_INTRO 진입으로 정의하면 이벤트로 추적 가능.
- **활동 1회 이상 수행**  
  - `game_start` / `game_intro` 와 GAME_RESULT 진입 또는 “다시 도전하기” 직전까지의 플레이 이력으로 정의 가능.

→ **구현만 보면** 위 4개 지표는 모두 “한 번의 세션” 단위로 로그 기반 계산이 가능한 구조임.

### MVP Non-Goals 준수
- 계정/로그인, 결제/구독, 추천 AI, 히스토리/진도, 가족 시나리오, 알림, 백엔드 필수, 스토어 배포 등은 PRD상 제외.  
- 현재 코드에도 해당 기능들이 들어가 있지 않아 Non-Goals와 맞음.

### 나중에 바라볼 BM·확장 포인트 (PRD 범위 외)
- 프리미엄 화가/작품, “오늘의 한 편” 같은 짧은 콘텐츠, 가족이 함께 보는 대시보드 등은 문서에도 “장기 비전”으로만 언급.  
- 현재 MVP는 “Screening → Feedback → Activity 전환” 검증에 맞게 잘 잘라져 있음.

---

## 5. 요약·권장 액션

### 플로우 적합성 (MVP 기준)
- **전반**: Screening → Feedback → Activity 퍼널이 끊김 없이 이어지고,  
  튜닝 뒤로가기·명화 클립 옵션·게임 결과 화면까지 포함하면 **“한 번에 검증하고 싶은 흐름”**은 갖춰져 있음.

### 우선 정리 권장
1. **감정 일기 완전 제거**  
   - `EMOTION_DIARY` / `DIARY_LIST` 렌더 분기, `EmotionDiaryScreen` / `DiaryListScreen` import·export,  
     `skipFlow` 등 내부 참조를 제거.  
   - 그동안 “감정 일기 모아보기”만 숨긴 상태라면, 이번에 “화면·상태·액션”까지 한꺼번에 제거해 두는 것이 플로우 해석과 유지보수에 유리함.

### 선택적 개선 (우선순위 낮음)
2. Result와 Hub에서 “명화 클립 같이 보기” 사용처·문구를 한 번 더 맞추기.  
3. 용어 통일(“세션 vs 게임 vs 아트 세션”)과 버튼 라벨 정리.  
4. GAME_RESULT 이후 “같은 화가 다른 게임” 숏컷 추가 여부를 사용성 테스트 결과 보고 결정.

### 결론
- **UI/UX**: 퍼널·뒤로가기·옵션 노출은 MVP 목적에 부합. 문구·용어·접근성만 다듬으면 됨.  
- **서비스(플로우)**: 스크리닝·매칭·진입 로직은 일관됨. 감정 일기 관련 데드 코드만 제거하면 “현재 서비스 = 현재 코드”로 정렬됨.  
- **사업성(MVP)**: PRD 가설 1~3과 Success Criteria를 이벤트 기반으로 측정할 수 있는 구조이며, Non-Goals도 지키고 있어서 **MVP 기준으로 플로우는 괜찮은 상태**로 보는 것이 타당함.
